# switch between cluster es,kibana,etc
# Display the node heap stats.
vulcanizer heap --cluster es

# Display the indices of the cluster.
vulcanizer indices --cluster es

# Get shard data by cluster node(s).
vulcanizer shards --cluster es

# Set shard allocation on the cluster.
vulcanizer allocation --cluster es

# Display the health of the cluster.
vulcanizer health --cluster es